<%= simple_form_for(@maker) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>
  <div class="form-inputs">
    <%= f.button :submit, :disable_with => '送信中...' %>
  </div>
<% end %>
