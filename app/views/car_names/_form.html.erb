<%= simple_form_for @car_name, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.association :maker %>
  <%= f.input :name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                car_names_path, :class => 'btn' %>
  </div>
<% end %>
